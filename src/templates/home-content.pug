main.content
  section.home--about-section
    article.latest--post
      div.article--header
        a(href="/posts/" + path) #[h2=attributes.title]
        span.post--meta #[+time(attributes.date)]
      div.article--content
        | !{preview}
        a(href="/posts/" + path, class="continue--reading") Continue reading
